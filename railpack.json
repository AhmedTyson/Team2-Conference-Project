{
  "$schema": "https://railpack.com/config.schema.json",
  "provider": "php",
  "build": {
    "command": "cd fullstack/Backend && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "bash start.sh"
  }
}
